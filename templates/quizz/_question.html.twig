<h2>{{ question.content }}</h2>

{{ form_start(form, { action: path('quiz_answer', { slug: quiz.slug, questionId: question.id }) }) }}
{{ form_widget(form.answer) }}
<button type="submit" class="btn btn-primary mt-3">Suivant</button>
{{ form_end(form) }}
