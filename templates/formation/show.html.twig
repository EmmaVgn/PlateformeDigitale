{% extends 'base.html.twig' %}

{% block title %}
	{{ formation.title }}
{% endblock %}

{% block body %}
	<h1>{{ formation.title }}</h1>
	{% if app.user %}
		{% if inscription %}
			<div style="padding: 1rem; border: 1px solid #cce5ff; background-color: #e6f0ff; margin-bottom: 1rem;">
				✅ Vous êtes inscrit(e) à cette formation.
				{% if inscription.progression > 0 %}
					<br>📊 Progression :
					{{ inscription.progression }}%
				{% endif %}
			</div>
		{% else %}
			<form method="post" action="{{ path('formation_inscription', { slug: formation.slug }) }}">
				<button type="submit">S'inscrire à cette formation</button>
			</form>
		{% endif %}
	{% else %}
		<p>
			<a href="{{ path('app_login') }}">Connectez-vous</a>
			pour vous inscrire.</p>
	{% endif %}


	<p>{{ formation.description }}</p>

	<h2>Modules</h2>
	{% if formation.modules|length > 0 %}
		<ul>
			{% for module in formation.modules %}
				<li>
					<h3>{{ module.title }}</h3>
					<p>{{ module.content|nl2br }}</p>
					{% if module.file %}
						<a href="{{ asset('uploads/files/' ~ module.file) }}" target="_blank">Télécharger</a>
					{% endif %}
				</li>
			{% endfor %}
		</ul>
	{% else %}
		<p>Aucun module pour cette formation.</p>
	{% endif %}

	<h2>Quiz</h2>
	{% if formation.quizzes|length > 0 %}
		<ul>
			{% for quiz in formation.quizzes %}
				<li>
					{{ quiz.title }}
					<a href="#">Lancer le quiz</a>
					{# lien à définir plus tard #}
				</li>
			{% endfor %}
		</ul>
	{% else %}
		<p>Pas de quiz associé pour le moment.</p>
	{% endif %}
{% endblock %}
